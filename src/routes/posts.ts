import { Router } from 'express';

const router = Router();

router.get('/', () => {});
// router.get('/:id', () => {});
// router.get('/admin', () => {});
// router.post('/', () => {});
// router.put('/:id', () => {});
// router.delete('/:id', () => {});

export { router };
